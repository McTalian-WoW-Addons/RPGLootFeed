<Ui xmlns="http://www.blizzard.com/wow/ui/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.blizzard.com/wow/ui/ ..\FrameXML\UI.xsd">
	<Script file="RowTimerBarMixin.lua"/>
	<Frame name="RLF_RowTimerBarTemplate" virtual="true" mixin="RLF_RowTimerBarMixin">
		<Frames>
			<!-- Timer bar StatusBar docked at the bottom of the row -->
			<!-- When enabled, appears as a thin bar that drains from right to left -->
			<!-- Starts hidden: only StartTimerBar() may show it, and only when the
			     feature is enabled. The BACKGROUND track below draws whenever this
			     StatusBar is shown, independent of the bar's value, so leaving it
			     visible would render a solid bar for disabled rows. -->
			<StatusBar parentKey="TimerBar" hidden="true">
				<Anchors>
					<Anchor point="BOTTOMLEFT" relativePoint="BOTTOMLEFT" />
					<Anchor point="BOTTOMRIGHT" relativePoint="BOTTOMRIGHT" />
				</Anchors>
				<BarTexture file="Interface/Buttons/WHITE8x8"/>
				<Layers>
					<Layer level="BACKGROUND">
						<Texture setAllPoints="true" file="Interface/Buttons/WHITE8x8">
							<Color r="0.5" g="0.5" b="0.5" a="0.7" />
						</Texture>
					</Layer>
				</Layers>
			</StatusBar>
		</Frames>
	</Frame>
</Ui>
