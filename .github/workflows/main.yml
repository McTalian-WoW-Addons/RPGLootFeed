name: Main

on:
  push:
    branches:
      - main
      - ([0-9]+)?(.{([0-9]+),x}).x # 1.x, 1.2.x, 1.x.x

permissions: {}

jobs:
  ci:
    permissions:
      contents: write
      actions: write
      pull-requests: write
      issues: write
    # trunk-ignore(pinact/unpinned-action)
    uses: McTalian-WoW-Addons/wow-build-tools/.github/workflows/ci.yml@7e264696b71fe78dcb2948c824f82ba18257e687 # v1
    with:
      addon-name: RPGLootFeed
      rockspec-name: rpglootfeed-1-1.rockspec
      lua-version: 5.4.4
      i18n-enabled: true
    secrets: inherit
