name: Close stale issues
on:
  schedule:
    - cron: 30 1 * * *

permissions: {}

jobs:
  stale:
    permissions:
      contents: read
      issues: write
      pull-requests: write
    # trunk-ignore(pinact/unpinned-action)
    uses: McTalian-WoW-Addons/wow-build-tools/.github/workflows/cleanup-stale-issues.yml@83b931fdae0211618c649340d6cf90c3262ef8bf # v1
